        # mx = 0
        # dig = 0
        # for j in range (len(currentRow)):
        #     [a, b, r] = currentRow[j]
        #     # if i == 6: cv2.circle(colored, (a, b), r, (0, 255, 0), 2)
        #     cnt = count_good_pixels(gray, a, b)
        #     if cnt > mx: 
        #         mx = cnt
        #         dig = j
        # [a, b, r] = currentRow[dig]
        # cv2.circle(colored, (a, b), r, (0, 255, 0), 2)
        # id+=str(dig)